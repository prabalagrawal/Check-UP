{
  "entities": {
    "users": {
      "title": "User Profile",
      "description": "Stores user basic information and health preferences.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Firebase Auth UID" },
        "email": { "type": "string", "format": "email" },
        "displayName": { "type": "string" },
        "role": { "type": "string", "enum": ["consumer", "doctor", "insurance"] },
        "medicalHistory": { "type": "string" },
        "lifestyle": {
          "type": "object",
          "properties": {
            "foodHabits": { "type": "string" },
            "exerciseFrequency": { "type": "string" },
            "smoking": { "type": "boolean" },
            "alcohol": { "type": "string" }
          }
        },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email", "role"]
    },
    "healthRecords": {
      "title": "Health Record",
      "description": "Medical history, lab reports, and prescriptions.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "type": { "type": "string", "enum": ["lab_report", "prescription", "diagnosis", "visit"] },
        "title": { "type": "string" },
        "date": { "type": "string", "format": "date" },
        "content": { "type": "string" },
        "fileUrl": { "type": "string" },
        "verified": { "type": "boolean" }
      },
      "required": ["userId", "type", "title", "date"]
    },
    "wearableData": {
      "title": "Wearable Data",
      "description": "Daily health metrics from wearables.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "date": { "type": "string", "format": "date" },
        "steps": { "type": "integer" },
        "heartRate": { "type": "integer" },
        "sleepHours": { "type": "number" },
        "stressLevel": { "type": "integer" }
      },
      "required": ["userId", "date"]
    },
    "riskPredictions": {
      "title": "Risk Prediction",
      "description": "AI-generated health risk assessments.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "predictionDate": { "type": "string", "format": "date-time" },
        "category": { "type": "string" },
        "riskScore": { "type": "number" },
        "insights": { "type": "string" },
        "recommendations": { "type": "array", "items": { "type": "string" } }
      },
      "required": ["userId", "predictionDate", "category", "riskScore"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "users",
      "description": "User profile document"
    },
    "/healthRecords/{recordId}": {
      "schema": "healthRecords",
      "description": "Individual health records"
    },
    "/wearableData/{dataId}": {
      "schema": "wearableData",
      "description": "Daily wearable metrics"
    },
    "/riskPredictions/{predictionId}": {
      "schema": "riskPredictions",
      "description": "AI risk predictions"
    },
    "/journalEntries/{entryId}": {
      "schema": "journalEntries",
      "description": "Daily health journal entries with AI insights"
    },
    "/vitals/{vitalId}": {
      "schema": "vitals",
      "description": "Daily calorie and water tracking"
    }
  },
  "entities": {
    "users": {
      "title": "User Profile",
      "description": "Stores user basic information and health preferences.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Firebase Auth UID" },
        "email": { "type": "string", "format": "email" },
        "displayName": { "type": "string" },
        "role": { "type": "string", "enum": ["consumer", "doctor", "insurance"] },
        "medicalHistory": { "type": "string" },
        "lifestyle": {
          "type": "object",
          "properties": {
            "foodHabits": { "type": "string" },
            "exerciseFrequency": { "type": "string" },
            "smoking": { "type": "boolean" },
            "alcohol": { "type": "string" }
          }
        },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email", "role"]
    },
    "healthRecords": {
      "title": "Health Record",
      "description": "Medical history, lab reports, and prescriptions.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "type": { "type": "string", "enum": ["lab_report", "prescription", "diagnosis", "visit"] },
        "title": { "type": "string" },
        "date": { "type": "string", "format": "date" },
        "content": { "type": "string" },
        "fileUrl": { "type": "string" },
        "verified": { "type": "boolean" }
      },
      "required": ["userId", "type", "title", "date"]
    },
    "wearableData": {
      "title": "Wearable Data",
      "description": "Daily health metrics from wearables.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "date": { "type": "string", "format": "date" },
        "steps": { "type": "integer" },
        "heartRate": { "type": "integer" },
        "sleepHours": { "type": "number" },
        "stressLevel": { "type": "integer" }
      },
      "required": ["userId", "date"]
    },
    "riskPredictions": {
      "title": "Risk Prediction",
      "description": "AI-generated health risk assessments.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "predictionDate": { "type": "string", "format": "date-time" },
        "category": { "type": "string" },
        "riskScore": { "type": "number" },
        "insights": { "type": "string" },
        "recommendations": { "type": "array", "items": { "type": "string" } }
      },
      "required": ["userId", "predictionDate", "category", "riskScore"]
    },
    "journalEntries": {
      "title": "Journal Entry",
      "description": "Daily activity and symptom log with AI analysis.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "entry": { "type": "string" },
        "date": { "type": "string", "format": "date-time" },
        "insight": {
          "type": "object",
          "properties": {
            "analysis": { "type": "string" },
            "potentialIssues": { "type": "array", "items": { "type": "string" } },
            "matchingSymptoms": { "type": "array", "items": { "type": "string" } },
            "regulationAdvice": { "type": "string" },
            "suggestedNextSteps": { "type": "string" }
          }
        }
      },
      "required": ["userId", "entry", "date"]
    },
    "vitals": {
      "title": "Daily Vitals",
      "description": "Calorie and water tracking.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "calories": { "type": "integer" },
        "water": { "type": "integer" },
        "date": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "date"]
    }
  }
}
